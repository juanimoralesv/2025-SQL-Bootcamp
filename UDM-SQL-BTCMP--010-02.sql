/* |||| Used database is "learning" |||| */

/* 79. Import and Export */
/* Table creation for later data import */
CREATE TABLE simplet(
	a INTEGER,
	b INTEGER,
	c INTEGER
);

/* Verify table exists */
SELECT *
FROM simplet;
/* After data import, verify again  */
